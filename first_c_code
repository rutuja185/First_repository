#include<stdio.h>
int main() {
  int i;                
  int arr[10];              
  for(int i=0;i<maxlen;i++){
    arr[i]=rand()%100;
    }
  int sum=0;
  for(int i=0;i<maxlen;i++)
  {
    printf("%d\n",arr[i]);
    sum+=arr[i];
  }
  printf("Sum of array elements is %d\n",sum);
  return 0;
}
